# HA webhook overlay (use with webhook.enabled=true).
#   helm upgrade --install ... -f values-webhook-only.yaml -f values-webhook-ha.yaml
# or with values-incluster.yaml.
webhook:
  replicaCount: 2
  minAvailable: 1
  failurePolicy: Fail
  certManager:
    enabled: true
